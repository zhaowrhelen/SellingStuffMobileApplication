import { createStackNavigator } from "@reaact-navigation/stack";

const Stack = createStackNavigator();

const AuthNavigator = () => (
  <Stack.Navigator>
    <Stack.Screen />
    <Stack.Screen />
    <Stack.Screen />
  </Stack.Navigator>
);
